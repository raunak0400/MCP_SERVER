# Auxiliary server Dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build --prefix .. || true
EXPOSE 4000
CMD ["node", "dist/server/index.js"]
